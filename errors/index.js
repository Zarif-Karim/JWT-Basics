const CustomAPIError = require('./custom-error');
const BadRequestError = require('./bad-request');
const UnauthorisedError = require('./unauthorised');

module.exports = {
    CustomAPIError, BadRequestError, UnauthorisedError
};